<a href="/" class="logo-cube">
  <div class="cube-wrapper">
    <span class="face front">0x6576616E</span>
    <span class="face bottom">Evan</span>
  </div>
</a>

<style>
  .logo-cube {
    display: inline-block;
    text-decoration: none;
    perspective: 1000px;
    font-family: 'Courier New', monospace;
    font-weight: bold;
    cursor: pointer;
    
    /* 核心修复：扩大交互热区 */
    padding: 10px 0; /* 上下增加 10px 的安全区 */
    margin: -10px 0; /* 抵消 padding 对布局的影响 */
    
    /* 核心修复：锁定宽度，防止文字长短不一导致闪烁 */
    min-width: 11ch; 
  }

  .cube-wrapper {
    position: relative;
    transition: transform 0.5s cubic-bezier(0.2, 0.8, 0.2, 1);
    transform-style: preserve-3d;
    height: 1.5em;
    width: 100%; /* 填满 min-width */
  }

  /* 只有鼠标真正悬停在扩大的区域内时才翻转 */
  .logo-cube:hover .cube-wrapper {
    transform: rotateX(90deg);
  }

  .face {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    backface-visibility: hidden;
    display: flex;
    align-items: center;
    /* 保持文字左对齐，视觉上更稳定，或者改为 center */
  }

  /* 正面：0x6576616E */
  .front {
    color: #fff;
    transform: translateZ(0.75em);
    background: transparent;
  }

  /* 底面：Evan */
  .bottom {
    color: #b026ff;
    transform: rotateX(-90deg) translateZ(0.75em);
    text-shadow: 0 0 10px rgba(176, 38, 255, 0.8);
    letter-spacing: 2px;
    /* 让 Evan 在宽容器里居中看起来更好，或者保持左对齐 */
    padding-left: 2px; 
  }
</style>